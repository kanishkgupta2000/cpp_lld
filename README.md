# DDIA practice

This repository contains implementation of DDIA concepts, along with C++ concept practice.

Currently covers:
1. Building a HTTP-TCP server in C++ using the linux socket APIs. 
2. Building a LSM-Tree database.